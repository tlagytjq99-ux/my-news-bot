name: Global AI Policy Monitoring

on:
  schedule:
    - cron: '0 0 * * *' # 매일 자정(KST 오전 9시) 자동 실행
  workflow_dispatch: # 수동 실행 버튼 활성화

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Libraries
        run: |
          pip install requests googletrans==4.0.0-rc1 feedparser googlenewsdecoder

      - name: Run Global AI Monitor
        run: python global_ai_monitor.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add global_ai_policy_monitor.csv
          git diff --quiet && git diff --staged --quiet || (git commit -m "📅 글로벌 AI 정책 동향 업데이트" && git push)
