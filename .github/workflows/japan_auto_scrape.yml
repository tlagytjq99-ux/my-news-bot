name: Japan AI Policy Auto Tracker

on:
  schedule:
    - cron: '0 0 * * *' # 매일 한국 시간(KST) 오전 9시 자동 실행
  workflow_dispatch:      # 필요할 때 버튼 눌러서 즉시 실행

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write    # 파일 저장을 위한 권한 설정

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install feedparser googletrans==4.0.0-rc1

      - name: Run Japan Scraper
        run: python japan_ai_crawler.py

      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          # 파일이 존재하는 경우에만 깃허브에 반영
          if [ -f japan_ai_report.csv ]; then
            git add japan_ai_report.csv
            git commit -m "🚀 [Update] 일본 내각부 AI 정책 리포트 갱신" || echo "변경 사항 없음"
            git push
          else
            echo "업데이트할 데이터 파일이 없습니다."
          fi
