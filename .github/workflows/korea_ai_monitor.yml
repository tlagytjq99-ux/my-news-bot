name: Korea AI Policy Auto Monitor

on:
  schedule:
    # 한국 시간 기준 매일 오전 9시 (UTC 00:00)
    - cron: '0 0 * * *'
  workflow_dispatch: 

permissions:
  contents: write

jobs:
  run-crawler:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 저장소 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🐍 파이썬 환경 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 🛠️ 필수 라이브러리 설치
        run: |
          pip install feedparser googlenewsdecoder requests

      - name: 🚀 국내 크롤러 실행 (정밀 필터 적용)
        run: python korea_ai_crawler.py

      - name: 💾 데이터 자동 커밋 및 저장
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add *.csv
          
          # 변경사항이 있을 때만 날짜와 함께 커밋
          NOW=$(date +'%Y-%m-%d %H:%M')
          if ! git diff --cached --quiet; then
            git commit -m "🇰🇷 국내 정책 정밀 업데이트 ($NOW)"
            git push
          else
            echo "새로 수집된 유의미한 데이터가 없습니다."
          fi
