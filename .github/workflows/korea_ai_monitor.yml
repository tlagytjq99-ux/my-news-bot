name: Korea AI Policy Auto Monitor

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch: 

permissions:
  contents: write

jobs:
  run-crawler:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 저장소 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🐍 파이썬 환경 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 🛠️ 필수 라이브러리 설치
        run: |
          pip install feedparser googlenewsdecoder requests

      - name: 🚀 국내 크롤러 실행
        run: |
          python korea_ai_crawler.py
          # 파일이 잘 생성되었는지 목록 확인 (디버깅용)
          ls -l *.csv || echo "파일이 생성되지 않았습니다."

      - name: 💾 데이터 업데이트 및 커밋
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # 현재 디렉토리에 있는 모든 csv 파일을 추가
          git add *.csv
          
          # 날짜 변수 설정
          NOW=$(date +'%Y-%m-%d %H:%M')
          
          # 변경사항(스테이징된 파일)이 있는지 확인 후 커밋
          if ! git diff --cached --quiet; then
            git commit -m "🇰🇷 국내 정책 데이터 업데이트 ($NOW)"
            git push
          else
            echo "업데이트할 새로운 데이터가 없습니다."
          fi
