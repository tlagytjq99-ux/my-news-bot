name: Korea AI Policy Auto Monitor

on:
  schedule:
    # 한국 시간 기준 매일 오전 9시 (UTC 00:00) 실행
    - cron: '0 0 * * *'
  workflow_dispatch: # 수동 실행 버튼 활성화

permissions:
  contents: write

jobs:
  run-crawler:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 저장소 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🐍 파이썬 환경 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 🛠️ 필수 라이브러리 설치
        run: |
          pip install feedparser googlenewsdecoder requests

      - name: 🚀 국내 크롤러 실행
        run: python korea_ai_crawler.py

      - name: 💾 데이터 자동 업데이트 및 커밋
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add korea_ai_policy_report.csv
          git diff --quiet && git diff --staged --quiet || (git commit -m "🇰🇷 국내 정책 데이터 업데이트 (${{ strftime('%Y-%m-%d') }})" && git push)
