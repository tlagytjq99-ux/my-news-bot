name: Market Intelligence Scraper

on:
  workflow_dispatch: # 👈 이 버튼 설정만 남겨두었습니다. 이제 수동으로만 돌아갑니다.
  # schedule:
  #   - cron: '0 0 * * *' # 필요할 때 앞에 '#'을 지우면 매일 아침 자동 실행됩니다.

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install feedparser googletrans==4.0.0-rc1

      - name: Run Scraper
        run: python mckinsey_ai_crawler.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add .
          git commit -m "Update Market Intelligence Report" || echo "No changes to commit"
          git push
