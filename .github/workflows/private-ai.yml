name: Private Consulting AI Watch

on:
  schedule:
    - cron: '0 1 * * *' # 매일 오전 1시(KST 오전 10시)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-crawler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install
        run: pip install requests googletrans==4.0.0-rc1 feedparser googlenewsdecoder
      - name: Run
        run: python private_consulting_ai.py
      - name: Commit
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add private_consulting_ai_monitor.csv
          git commit -m "💼 민간 AI 리포트 업데이트" || exit 0
          git push
