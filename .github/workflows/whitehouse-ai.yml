name: White House AI Policy Watch

on:
  schedule:
    - cron: '30 0 * * *' # 매일 자정 30분(KST 오전 9시 30분) 자동 실행
  workflow_dispatch:

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Libraries
        run: |
          pip install requests googletrans==4.0.0-rc1 feedparser googlenewsdecoder

      - name: Run White House Monitor
        run: python whitehouse_ai_monitor.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add whitehouse_ai_policy.csv
          git diff --quiet && git diff --staged --quiet || (git commit -m "🇺🇸 백악관 AI 신규 정책 업데이트" && git push)
